node_modules/
dist/
server/data/
.DS_Store
farm/.venv/
__pycache__/
*.pyc
.pytest_cache/
e2e/test-results/
e2e/playwright-report/
e2e/blob-report/
# HZ-63: screenshots are published per-PR to a git ref, not committed —
# binary files have no merge strategy, so tracking them made every branch
# that touched the same journey conflict with every other.
e2e/__screenshots__/
